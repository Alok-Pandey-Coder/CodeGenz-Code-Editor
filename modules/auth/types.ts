export interface LogoutButtonProps{
  children?: React.ReactNode
}

export type newData = {
   userName: string,
    Avatar: string ,
    organization: string,
    experience: string,
    location: string,
    readMe: string,
    birthDate: Date,
    gender: string,
}